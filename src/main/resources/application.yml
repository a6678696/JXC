server:
  port: 80
  servlet:
    context-path: /
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/db_jxc?serverTimezone=UTC
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false